from src.occlusion_heuristic import heuristic_complete

def amodal_experimental(mask, label):
    # Temporary experimental fallback until you choose a concrete amodal checkpoint.
    return heuristic_complete(mask, label)